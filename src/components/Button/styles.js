import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
  },
  wrapper: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  text: {
    color: '#e2e2e2',
    paddingVertical: 8,
    fontSize: 16,
    fontWeight: '300',
  },
  img: {
    width: 25,
    marginRight: 11,
  },
});

export default styles;
