import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    textAlign: 'center',
    fontWeight: '600',
    fontSize: 16,
    color: '#e2e2e2',
  },
});

export default styles;
